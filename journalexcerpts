import React, { useState } from 'react';
import { ChevronLeft, ChevronRight, Book, Skull, MapPin, Search } from 'lucide-react';

// --- Data: The Journal Entries ---
const journalEntries = [
  {
    id: "cover",
    type: "cover",
    title: "Field Journal",
    subtitle: "Property of Jace Vern",
    details: ["Year 3 of the Thaw", "Major: Arcane Archaeology", "(Self-Assigned)"],
    warning: "IF FOUND: Return to Scholia Diaspros Administration. Do not read.",
  },
  {
    id: "entry-14",
    type: "content",
    title: "Entry 14",
    date: "The Mud",
    content: `The mud out here is different. Closer to the campus, the ground is soft, almost forgiving—like it wants to cradle you. Out here, past the boundary markers where the Hall Monitors stop patrolling, the mud is hungry.

I’ve read the old records—the ones the Headmistress hasn’t scrubbed yet. Before the War, before the Well cracked and the sky turned the color of a bruised plum, there was a township here. Real buildings. Stone and mortar, not just magic and intent. They say the "Sunken Tower" was the nexus of it all. Old Magitek. Not the soft, puffy enchantments we weave into our undergarments now, but hard magic. Cold magic.

The rumors say it didn’t fall. They say it was pulled down when the earth liquefied during the initial blast. If the Thaw is real, and the magical frost is finally receding from the fens, the spire should be visible. I have my supplies. My pack is heavy, and I’ve double-layered my padding. If I get stuck out there, I’m not coming back for a change.`,
  },
  {
    id: "entry-16",
    type: "content",
    title: "Entry 16",
    date: "The Sighting",
    content: `I saw it. Or I think I saw it.

The fog is thick today—soupy. It smells of copper and old ozone. I was navigating a ridge of solid earth near the sinkhole when the mist parted. Just for a second. A jagged black tooth jutting out of the scum water. It wasn’t stone. It looked like metal, slick and unrusted despite centuries in the muck.

I tried to get closer, but the local wildlife is aggressive. Saw a Darkling—not the cute, pranking kind we deal with in the dorms. This thing was feral. Shadow-stuff leaking off it like oil. It was picking through a pile of debris that looked suspiciously like an old dormitory wall. I stayed hidden. My stealth spell held, but barely. My diaper is already soaked through, mostly from nerves. I need to find a dry patch to rest before I make the final push to the spire.

Why does no one talk about what this place was? Why is it just "The Old Ruins"? The architecture I saw... it looked like a machine. A massive, buried machine.`,
  },
  {
    id: "entry-17",
    type: "content",
    title: "Entry 17",
    date: "Inside",
    content: `I am inside. Or... on top of? It’s hard to tell where the swamp ends and the Tower begins.

I managed to rappel down onto a balcony that was half-submerged. The interior is surprisingly dry, sealed by some kind of pressure field that hums against my teeth. It’s freezing in here.

The rumors were wrong. It wasn’t a library, and it wasn’t a command center. I found a room full of glass tubes. Most are shattered, but the ones that aren't... they have pipes running into the floor. Massive, brass pipes. The scale of this place is wrong. The doors are too tall. The chairs—if that’s what these metal frames are—are uncomfortable, rigid.

There’s a console here. It’s flickering. The text is in Old Common, blocked and sharp. I can make out the words "Regulation" and "Disposal."

I don't like the hum. It sounds like a heartbeat. A very slow, very deep heartbeat coming from beneath the water line. I’m going to try to copy the runes on the console and leave. This doesn't feel like a place of learning. It feels like a factory.`,
  },
  {
    id: "entry-18",
    type: "content",
    title: "Entry 18",
    date: "TRAPPED",
    content: `The door sealed.

I didn't touch it. I swear. I was sketching the rune sequence for the "Disposal" valve when the pressure field fluctuated. The air pressure dropped, my ears popped, and the bulkhead behind me slammed shut.

The humming has stopped. That’s worse. Silence in a place like this means something is listening.

I can hear scratching in the vents. It sounds metallic. Clinking. Like claws on brass. I tried to cast Knock on the door, but the mana just... dissolved. The walls are eating the magic.

I’m tired. I’m cold. I have one clean diaper left in my bag, but I’m too terrified to change. If I take my armor off, whatever is in those vents will smell the vulnerability.

The scratching is getting closer. It sounds like... giggling? But distorted. Tinny.

If anyone finds this, don't look for the lever. Do not pull the lever marked "Sanitization."

I think the Tower isn't a building. I think it's a—`,
  }
];

export default function JaceJournalApp() {
  const [currentPage, setCurrentPage] = useState(0);

  const nextPage = () => {
    if (currentPage < journalEntries.length - 1) {
      setCurrentPage(curr => curr + 1);
    }
  };

  const prevPage = () => {
    if (currentPage > 0) {
      setCurrentPage(curr => curr - 1);
    }
  };

  const currentEntry = journalEntries[currentPage];

  return (
    <div className="min-h-screen bg-stone-950 flex items-center justify-center p-4 font-serif overflow-hidden relative">
      {/* Background Ambience */}
      <div className="absolute inset-0 opacity-20 pointer-events-none" 
           style={{
             backgroundImage: `url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%233e2723' fill-opacity='1'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E")`
           }}>
      </div>

      {/* The Journal Container */}
      <div className="relative w-full max-w-2xl perspective-1000">
        
        {/* Book Spine Visuals (Left Side Decor) */}
        <div className="absolute -left-2 top-4 bottom-4 w-6 bg-amber-950 rounded-l-lg shadow-2xl z-0 border-l border-t border-b border-amber-800"></div>

        {/* Main Page Area */}
        <div className="relative bg-[#e8dcc5] w-full min-h-[600px] rounded-r-lg shadow-[0_20px_50px_-12px_rgba(0,0,0,0.9)] flex flex-col border-r-4 border-b-4 border-[#c4b495]">
          
          {/* Paper Texture Overlay */}
          <div className="absolute inset-0 opacity-40 pointer-events-none rounded-r-lg"
               style={{
                 backgroundImage: `linear-gradient(to right, rgba(62, 39, 35, 0.1), rgba(0,0,0,0) 10%), url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.1'/%3E%3C/svg%3E")`
               }}>
          </div>

          {/* Gold Trim Top */}
          <div className="h-2 w-full bg-amber-900/80 border-b border-amber-400/50 mx-auto mt-4 w-[90%]"></div>

          {/* Content Area */}
          <div className="flex-1 p-8 sm:p-12 relative z-10 flex flex-col text-stone-900">
            
            {currentEntry.type === 'cover' ? (
              // COVER PAGE DESIGN
              <div className="flex flex-col items-center justify-center h-full text-center space-y-8 border-4 border-double border-stone-800 p-8 m-4 bg-[#dccba6] shadow-inner">
                 <Book className="w-16 h-16 text-amber-900 mb-4" />
                 <div>
                   <h1 className="text-4xl font-bold tracking-widest text-stone-900 uppercase border-b-2 border-stone-800 pb-2 mb-2">{currentEntry.title}</h1>
                   <h2 className="text-xl text-stone-700 italic font-semibold">{currentEntry.subtitle}</h2>
                 </div>
                 
                 <div className="space-y-2 text-stone-800">
                   {currentEntry.details?.map((detail, idx) => (
                     <p key={idx} className="text-sm font-medium tracking-wide uppercase">{detail}</p>
                   ))}
                 </div>

                 <div className="mt-12 p-4 border-2 border-red-900/30 bg-red-900/5 text-red-900 text-xs font-bold tracking-widest uppercase w-full">
                    <Skull className="w-4 h-4 inline-block mr-2 mb-1" />
                    {currentEntry.warning}
                 </div>
              </div>
            ) : (
              // CONTENT PAGE DESIGN
              <div className="flex flex-col h-full animate-in fade-in duration-500">
                <div className="flex justify-between items-end border-b-2 border-stone-400/50 pb-2 mb-6">
                  <h2 className="text-2xl font-bold text-amber-950">{currentEntry.title}</h2>
                  <span className="text-sm font-mono text-stone-500 uppercase tracking-tight flex items-center gap-1">
                    <MapPin className="w-3 h-3" /> {currentEntry.date}
                  </span>
                </div>

                <div className="flex-1 overflow-y-auto pr-2 custom-scrollbar">
                  {currentEntry.content?.split('\n\n').map((paragraph, idx) => (
                    <p key={idx} className="mb-4 text-lg leading-relaxed text-stone-800 indent-4 first-letter:text-3xl first-letter:font-bold first-letter:text-amber-900 first-letter:mr-1">
                      {paragraph}
                    </p>
                  ))}
                  
                  {/* Last entry specific decoration */}
                  {currentEntry.id === "entry-18" && (
                    <div className="mt-8 border-t border-stone-400 pt-4 text-center">
                      <p className="text-red-900/70 text-sm italic font-mono tracking-widest">
                        [ END OF RECORD ]
                      </p>
                      <div className="flex justify-center gap-2 mt-2 opacity-50">
                        <span className="w-2 h-2 bg-stone-800 rounded-full animate-pulse"></span>
                        <span className="w-2 h-2 bg-stone-800 rounded-full animate-pulse delay-75"></span>
                        <span className="w-2 h-2 bg-stone-800 rounded-full animate-pulse delay-150"></span>
                      </div>
                    </div>
                  )}
                </div>
              </div>
            )}

            {/* Page Number Footer */}
            <div className="mt-auto pt-6 flex justify-center text-stone-400 text-xs font-mono">
              Page {currentPage + 1} of {journalEntries.length}
            </div>

          </div>

          {/* Gold Trim Bottom */}
          <div className="h-2 w-full bg-amber-900/80 border-t border-amber-400/50 mx-auto mb-4 w-[90%]"></div>
        </div>

        {/* Navigation Buttons */}
        <div className="absolute -bottom-16 left-0 right-0 flex justify-center gap-8 z-20">
          <button 
            onClick={prevPage}
            disabled={currentPage === 0}
            className={`
              flex items-center justify-center w-12 h-12 rounded-full 
              bg-stone-800 border-2 border-amber-700 text-amber-100
              shadow-lg transition-all duration-200
              ${currentPage === 0 ? 'opacity-50 cursor-not-allowed' : 'hover:bg-stone-700 hover:scale-110 active:scale-95'}
            `}
          >
            <ChevronLeft className="w-6 h-6" />
          </button>
          
          <button 
            onClick={nextPage}
            disabled={currentPage === journalEntries.length - 1}
            className={`
              flex items-center justify-center w-12 h-12 rounded-full 
              bg-stone-800 border-2 border-amber-700 text-amber-100
              shadow-lg transition-all duration-200
              ${currentPage === journalEntries.length - 1 ? 'opacity-50 cursor-not-allowed' : 'hover:bg-stone-700 hover:scale-110 active:scale-95'}
            `}
          >
            <ChevronRight className="w-6 h-6" />
          </button>
        </div>

        {/* Decorative elements for the table/desk vibe */}
        <div className="absolute -right-8 top-1/4 w-24 h-24 bg-black/20 rounded-full blur-xl pointer-events-none"></div>
      </div>
      
      <style jsx global>{`
        .custom-scrollbar::-webkit-scrollbar {
          width: 6px;
        }
        .custom-scrollbar::-webkit-scrollbar-track {
          background: rgba(0,0,0,0.05);
        }
        .custom-scrollbar::-webkit-scrollbar-thumb {
          background: rgba(62, 39, 35, 0.3);
          border-radius: 10px;
        }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
          background: rgba(62, 39, 35, 0.5);
        }
      `}</style>
    </div>
  );
}